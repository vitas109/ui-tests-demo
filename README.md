# Galaxy Store 🌌

Интернет-магазин атрибутики по вселенной "Звёздные войны" с UI тестами на TypeScript + Playwright и Allure отчётностью.

## 🌐 Деплой

- **Основной сайт:** https://vitas109.github.io/ui-tests-demo/
- **Allure отчёт:** https://vitas109.github.io/ui-tests-demo/allure-report

## 🚀 Возможности

- **Каталог товаров** - 16 товаров (световые мечи, одежда, мерч)
- **Фильтрация и сортировка** - по категории, цене, популярности и остатку
- **Корзина** - добавление/удаление товаров, сохранение в localStorage
- **Отзывы** - возможность оставить отзыв на купленный товар
- **Личный кабинет** - история заказов и отзывов
- **Авторизация** - с хардкодными учётными данными
- **Промокоды** - SW10 даёт скидку 10%
- **Оплата** - тестовый платёж (карта 1111 1111 1111 1111)

## 📋 Технологии

- **Frontend**: HTML, CSS, TypeScript
- **Build**: Vite
- **Linter**: ESlint
- **Testing**: TypeScript + Playwright
- **Test Reporting**: Allure
- **CI/CD**: GitHub Actions

## 🛠️ Установка

```bash
# Клонирование репозитория
git clone https://github.com/vitas109/ui-tests-demo.git
cd ui-tests-demo

# Установка зависимостей
npm install

# Установка Playwright браузеров
npx playwright install chromium
```

## ▶️ Запуск

### Development сервер
```bash
npm run dev
```
Откроет http://localhost:3000

### Production сборка
```bash
npm run build
```
Создаст папку `dist/` с готовыми файлами.

### Локальный preview
```bash
npm run preview
```

## 🧪 Запуск тестов

### Все тесты
```bash
npm run test
```

### Тесты с UI режимом
```bash
npm run test:ui
```

### Генерация Allure отчёта локально
```bash
npm run test
npm run allure:generate
npm run allure:open
```

Тесты запускаются автоматически при push в main через GitHub Actions.

## 🔐 Тестовые данные

### Авторизация
- **Email**: `qwe@qwe.ru`
- **Пароль**: `qweQWE`

### Промокод
- **Код**: `SW10`
- **Скидка**: 10%

### Оплата (тестовая)
- **Успешная карта**: `1111 1111 1111 1111`
- **Отклонённая карта**: Любая другая

## 📁 Структура проекта

```
├── src/
│   ├── main.ts              # Основная логика приложения
│   ├── styles/
│   │   └── main.css         # Стили
│   ├── utils/
│   │   ├── types.ts         # TypeScript типы
│   │   ├── data.ts          # Данные товаров
│   │   └── storage.ts       # LocalStorage функции
│   └── tests/
│       ├── app.spec.ts      # Основные тесты
│       └── pages/           # Page Objects
│           ├── index.ts
│           ├── BasePage.ts
│           ├── HomePage.ts
│           ├── CatalogPage.ts
│           ├── CartPage.ts
│           ├── AuthPage.ts
│           ├── ProfilePage.ts
│           ├── CheckoutPage.ts
│           └── ResultPage.ts
├── index.html               # Главная страница
├── package.json             # Зависимости
├── tsconfig.json            # TypeScript конфиг
├── vite.config.ts           # Vite конфиг
├── playwright.config.ts      # Playwright конфиг
└── .github/
    └── workflows/
        └── deploy.yml       # GitHub Actions
```

## 🧪 Тестовая отчётность (Allure)

Проект использует **Allure** для генерации красивых и информативных отчётов о тестировании.

### Просмотр отчёта

- **Онлайн:** https://vitas109.github.io/ui-tests-demo/allure-report
- **Локально:**
  ```bash
  npm run test
  npm run allure:generate
  npm run allure:open
  ```

### CI/CD интеграция

GitHub Actions автоматически:
1. Запускает тесты
2. Генерирует Allure отчёт
3. Публикует отчёт на GitHub Pages

## 🧪 Структура тестов (Page Object Model)

Тесты используют паттерн **Page Object Model (POM)** для лучшей поддержки и читаемости кода.

## ✅ Преимущества Page Object Model

1. **Инкапсуляция** - скрытие деталей реализации UI
2. **Повторное использование** - методы используются в разных тестах
3. **Поддержка** - легче поддерживать при изменениях UI
4. **Читаемость** - тесты становятся более понятными
5. **Масштабируемость** - легко добавлять новые тесты и страницы


### Page Objects

Каждый класс Page Object инкапсулирует взаимодействие с элементами конкретной страницы:

| Класс | Страница | Описание |
|-------|----------|----------|
| `BasePage` | - | Базовый класс с общими методами |
| `HomePage` | Главная | Логотип, hero-секция, карточки категорий |
| `CatalogPage` | Каталог | Товары, фильтры, сортировка |
| `CartPage` | Корзина | Товары, промокоды, оформление |
| `AuthPage` | Авторизация | Модальное окно входа/регистрации |
| `ProfilePage` | Личный кабинет | Профиль, заказы, отзывы |
| `CheckoutPage` | Оплата | Форма заполнения данных и оплата |
| `ResultPage` | Результат | Страницы успеха/отказа |

### Тестовые сценарии (23 теста)

#### Главная страница (2 теста)
- `должен загрузить главную страницу` - проверка logo, hero-текста, карточек
- `должен перейти на страницу каталога` - навигация в каталог

#### Каталог (6 тестов)
- `должен фильтровать товары по категории` - фильтрация по категориям
- `должен добавить товар в корзину` - добавление товара
- `должен показывать разные остатки товаров` - проверка разных остатков
- `должен сортировать по популярности` - сортировка
- `должен сортировать по остатку` - сортировка

#### Корзина (3 теста)
- `должен отобразить товары в корзине` - просмотр корзины
- `должен изменить количество товара в корзине` - изменение количества
- `должен удалить товар из корзины` - удаление товара

#### Авторизация (7 тестов)
- `должен открыть модальное окно авторизации` - открытие модалки
- `должен войти с корректными учетными данными` - успешный вход
- `должен отклонить неверные учетные данные` - неудачный вход
- `должен показать ошибку при пустом email` - валидация
- `должен показать ошибку при пустом пароле` - валидация
- `должен переключаться между входом и регистрацией` - переключение режимов

#### Оплата (2 теста)
- `должен перейти на страницу оплаты с валидной картой` - успешная оплата
- `должен отклонить платеж с невалидной картой` - отклонённая оплата

#### Личный кабинет (4 теста)
- `должен показать сообщение о необходимости входа` - проверка редиректа
- `должен перенаправить на авторизацию` - кнопка входа
- `должен отобразить информацию о пользователе после входа` - профиль
- `должен выйти из аккаунта со страницы профиля` - выход

#### Навигация (1 тест)
- `должен переключаться между страницами` - общая навигация

## 🌐 GitHub Pages

Проект автоматически деплоится на GitHub Pages при push в main branch.
